#include <stdio.h>

int main() {
    int x = 10, y = 5;

    // Simple and straightforward logic
    int sum = x + y;
    printf("Sum: %d\n", sum);

    int diff = x - y;
    printf("Difference: %d\n", diff);

    return 0;
}
